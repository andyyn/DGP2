using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Example : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //in here is where we will have most of our stuff (since we have a visual novel)
    }

    // Update is called once per frame
    void Update()
    {
        //this is for any button clicks or changes or animations we want to add
    }

}
